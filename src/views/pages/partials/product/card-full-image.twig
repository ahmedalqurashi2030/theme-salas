{# Tharaa - Full Image Product Card (patched)
   Uses the same JS web-component card so behavior remains consistent.
#}

<custom-salla-product-card
  product="{{ product }}"
  fullImage
  shadowOnHover
></custom-salla-product-card>
