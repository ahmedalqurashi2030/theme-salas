{# Tharaa - Mini Product Card (patched)
   Uses the same JS web-component card so design stays consistent everywhere.
#}

<custom-salla-product-card
  product="{{ product }}"
  minimal
  shadowOnHover
></custom-salla-product-card>
