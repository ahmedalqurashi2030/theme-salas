{% set reviews = component.items|default([]) %}
{% set slider_id = 'custom-testimonials-' ~ (position ?? 'home') %}

{% set tr_section_title = trans('components.testimonials.section_title') %}
{% set tr_section_subtitle = trans('components.testimonials.section_subtitle') %}
{% set tr_customers_count = trans('components.testimonials.customers_count') %}
{% set tr_empty = trans('components.testimonials.empty') %}
{% set tr_customer_name = trans('components.testimonials.customer_name') %}
{% set tr_rating_prefix = trans('components.testimonials.rating_prefix') %}
{% set tr_rating_suffix = trans('components.testimonials.rating_suffix') %}

{% set default_section_title = tr_section_title starts with 'components.' ? 'Customer Testimonials' : tr_section_title %}
{% set default_section_subtitle = tr_section_subtitle starts with 'components.' ? 'Real feedback from customers who trusted our products and service.' : tr_section_subtitle %}
{% set default_customers_count = tr_customers_count starts with 'components.' ? '2k+ Happy Customers' : tr_customers_count %}
{% set default_empty_text = tr_empty starts with 'components.' ? 'No testimonials available yet.' : tr_empty %}
{% set default_customer_name = tr_customer_name starts with 'components.' ? 'Customer' : tr_customer_name %}
{% set rating_prefix = tr_rating_prefix starts with 'components.' ? 'Rating' : tr_rating_prefix %}
{% set rating_suffix = tr_rating_suffix starts with 'components.' ? 'out of 5' : tr_rating_suffix %}

{% set section_title = component.section_title|default(default_section_title) %}
{% if section_title is iterable %}
  {% set section_title = section_title.value|default(section_title.ar|default(section_title.en|default(default_section_title))) %}
{% endif %}

{% set section_subtitle = component.section_subtitle|default(default_section_subtitle) %}
{% if section_subtitle is iterable %}
  {% set section_subtitle = section_subtitle.value|default(section_subtitle.ar|default(section_subtitle.en|default(default_section_subtitle))) %}
{% endif %}

{% set customers_count_text = component.customers_count_text|default(default_customers_count) %}
{% if customers_count_text is iterable %}
  {% set customers_count_text = customers_count_text.value|default(customers_count_text.ar|default(customers_count_text.en|default(default_customers_count))) %}
{% endif %}

{% if attribute(component, 'show-stats') is defined %}
  {% set show_stats_raw = attribute(component, 'show-stats') %}
{% elseif attribute(component, 'show_stats') is defined %}
  {% set show_stats_raw = attribute(component, 'show_stats') %}
{% else %}
  {% set show_stats_raw = true %}
{% endif %}
{% if show_stats_raw is iterable %}
  {% if attribute(show_stats_raw, 'selected') is defined %}
    {% set show_stats_raw = attribute(show_stats_raw, 'selected') %}
  {% else %}
    {% set show_stats_raw = attribute(show_stats_raw, 'value')|default(show_stats_raw) %}
  {% endif %}
{% endif %}

{% if attribute(component, 'auto-play') is defined %}
  {% set auto_play_raw = attribute(component, 'auto-play') %}
{% elseif attribute(component, 'auto_play') is defined %}
  {% set auto_play_raw = attribute(component, 'auto_play') %}
{% else %}
  {% set auto_play_raw = true %}
{% endif %}
{% if auto_play_raw is iterable %}
  {% if attribute(auto_play_raw, 'selected') is defined %}
    {% set auto_play_raw = attribute(auto_play_raw, 'selected') %}
  {% else %}
    {% set auto_play_raw = attribute(auto_play_raw, 'value')|default(auto_play_raw) %}
  {% endif %}
{% endif %}

{% if attribute(component, 'show-pagination') is defined %}
  {% set show_pagination_raw = attribute(component, 'show-pagination') %}
{% elseif attribute(component, 'show_pagination') is defined %}
  {% set show_pagination_raw = attribute(component, 'show_pagination') %}
{% else %}
  {% set show_pagination_raw = true %}
{% endif %}
{% if show_pagination_raw is iterable %}
  {% if attribute(show_pagination_raw, 'selected') is defined %}
    {% set show_pagination_raw = attribute(show_pagination_raw, 'selected') %}
  {% else %}
    {% set show_pagination_raw = attribute(show_pagination_raw, 'value')|default(show_pagination_raw) %}
  {% endif %}
{% endif %}

{% if attribute(component, 'show_controls') is defined %}
  {% set show_controls_raw = attribute(component, 'show_controls') %}
{% elseif attribute(component, 'show-controls') is defined %}
  {% set show_controls_raw = attribute(component, 'show-controls') %}
{% else %}
  {% set show_controls_raw = true %}
{% endif %}
{% if show_controls_raw is iterable %}
  {% if attribute(show_controls_raw, 'selected') is defined %}
    {% set show_controls_raw = attribute(show_controls_raw, 'selected') %}
  {% else %}
    {% set show_controls_raw = attribute(show_controls_raw, 'value')|default(show_controls_raw) %}
  {% endif %}
{% endif %}

{% set show_stats = show_stats_raw in [true, 1, '1', 'true'] %}
{% set auto_play = auto_play_raw in [true, 1, '1', 'true'] %}
{% set show_pagination = show_pagination_raw in [true, 1, '1', 'true'] %}
{% set show_controls = show_controls_raw in [true, 1, '1', 'true'] %}

{% set has_multiple_reviews = reviews|length > 1 %}
{% set slider_show_controls = show_controls and has_multiple_reviews %}
{% set slider_show_pagination = show_pagination and has_multiple_reviews %}
{% set slider_auto_play = auto_play and has_multiple_reviews %}
{% set empty_reviews_text = default_empty_text %}

{% set slider_config = {
  "slidesPerView": 1,
  "spaceBetween": 14,
  "watchOverflow": true,
  "centeredSlides": false,
  "loop": reviews|length > 3,
  "autoplay": slider_auto_play ? {"delay": 3000, "disableOnInteraction": false} : false,
  "breakpoints": {
    "640": {
      "slidesPerView": 1.2,
      "spaceBetween": 14
    },
    "768": {
      "slidesPerView": 2,
      "spaceBetween": 16
    },
    "1024": {
      "slidesPerView": 3,
      "spaceBetween": 18
    }
  }
} %}

<section class="s-block s-block--testimonials th-home-testimonials container overflow-hidden">
  <div class="s-reviews-container">
    <div class="s-reviews-header-wrapper s-block__title">
      <div class="s-slider-block__title-right">
        <h2 class="s-reviews-header">{{ section_title }}</h2>
        {% if section_subtitle %}
          <p class="s-block__title-desc th-home-testimonials__subtitle">{{ section_subtitle }}</p>
        {% endif %}

        {% if show_stats %}
          <div class="th-home-testimonials__stats" aria-label="{{ customers_count_text }}">
            <div class="th-home-testimonials__stat">
              <span class="th-home-testimonials__avatars" aria-hidden="true">
                {% for item in reviews|slice(0, 4) %}
                  {% set stat_avatar = item.avatar|default(attribute(item, 'items.avatar')|default('')) %}
                  {% if stat_avatar is iterable %}
                    {% set stat_avatar = stat_avatar.url|default(stat_avatar.original|default(stat_avatar.value|default(''))) %}
                  {% endif %}
                  {% set stat_avatar = stat_avatar ?: ('images/s-empty-square.png' | asset) %}
                  <img src="{{ stat_avatar }}" loading="lazy" alt="" width="24" height="24" class="th-home-testimonials__avatar-chip">
                {% endfor %}
              </span>
              <span class="th-home-testimonials__stat-text">{{ customers_count_text }}</span>
            </div>
          </div>
        {% endif %}
      </div>
    </div>

    {% if reviews|length %}
      <salla-slider
        id="{{ slider_id }}"
        class="th-home-testimonials__slider testimonials-slider s-slider-wrapper"
        type="testimonials"
        show-controls="{{ slider_show_controls ? 'true' : 'false' }}"
        auto-play="{{ slider_auto_play ? 'true' : 'false' }}"
        slider-config='{{ slider_config|json_encode|e("html_attr") }}'
        {% if slider_show_controls %}controls-outer{% endif %}
        {% if slider_show_pagination %}pagination{% endif %}>
        <div slot="items">
          {% for index, item in reviews %}
            {% set reviewer_name = item.name|default(attribute(item, 'items.name')|default(default_customer_name)) %}
            {% if reviewer_name is iterable %}
              {% set reviewer_name = reviewer_name.value|default(reviewer_name.ar|default(reviewer_name.en|default(default_customer_name))) %}
            {% endif %}

            {% set reviewer_avatar = item.avatar|default(attribute(item, 'items.avatar')|default('')) %}
            {% if reviewer_avatar is iterable %}
              {% set reviewer_avatar = reviewer_avatar.url|default(reviewer_avatar.original|default(reviewer_avatar.value|default(''))) %}
            {% endif %}
            {% set reviewer_avatar = reviewer_avatar ?: ('images/s-empty-square.png' | asset) %}

            {% set reviewer_stars = item.stars|default(attribute(item, 'items.stars')|default(5)) %}
            {% if reviewer_stars is iterable %}
              {% set reviewer_stars = reviewer_stars.selected|default(reviewer_stars.value|default(5)) %}
            {% endif %}
            {% set reviewer_stars = reviewer_stars + 0 %}
            {% set reviewer_stars = reviewer_stars < 1 ? 1 : (reviewer_stars > 5 ? 5 : reviewer_stars) %}

            {% set reviewer_text = item.text|default(attribute(item, 'items.text')|default('')) %}
            {% if reviewer_text is iterable %}
              {% set reviewer_text = reviewer_text.value|default(reviewer_text.ar|default(reviewer_text.en|default(''))) %}
            {% endif %}

            {% set reviewer_rating_aria = rating_prefix ~ ' ' ~ reviewer_stars ~ ' ' ~ rating_suffix %}

            <div class="swiper-slide s-reviews-swiper-slide">
              <article class="s-reviews-testimonial">
                <div class="s-reviews-testimonial__inner">
                  <span class="s-reviews-testimonial__icon" aria-hidden="true">
                    <svg version="1.1" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
                      <title>quote-open</title>
                      <path d="M8 12v-5.333c0-0.737-0.596-1.333-1.333-1.333-3.676 0-6.667 2.991-6.667 6.667v8c0 3.676 2.991 6.667 6.667 6.667h1.333c3.676 0 6.667-2.991 6.667-6.667v-1.333c0-3.676-2.991-6.667-6.667-6.667zM12 20c0 2.205-1.795 4-4 4h-1.333c-2.205 0-4-1.795-4-4v-8c0-1.739 1.115-3.221 2.667-3.772v5.105c0 0.737 0.596 1.333 1.333 1.333h1.333c2.205 0 4 1.795 4 4zM25.333 12v-5.333c0-0.737-0.596-1.333-1.333-1.333-3.676 0-6.667 2.991-6.667 6.667v8c0 3.676 2.991 6.667 6.667 6.667h1.333c3.676 0 6.667-2.991 6.667-6.667v-1.333c0-3.676-2.991-6.667-6.667-6.667zM29.333 20c0 2.205-1.795 4-4 4h-1.333c-2.205 0-4-1.795-4-4v-8c0-1.739 1.115-3.221 2.667-3.772v5.105c0 0.737 0.596 1.333 1.333 1.333h1.333c2.205 0 4 1.795 4 4z"></path>
                    </svg>
                  </span>

                  <div class="s-reviews-testimonial__text">
                    <p>{{ reviewer_text }}</p>

                    <div class="s-reviews-testimonial__name_wrapper">
                      <span class="s-reviews-testimonial__avatar">
                        <img src="{{ reviewer_avatar }}" loading="lazy" decoding="async" width="68" height="68" alt="{{ reviewer_name|trim ? reviewer_name : 'testimonial-' ~ index }}">
                      </span>

                      <div class="s-reviews-testimonial__info">
                        <h3>{{ reviewer_name }}</h3>
                      </div>

                      <div class="s-reviews-testimonial__rating" aria-label="{{ reviewer_rating_aria }}">
                        <salla-rating-stars size="small" value="{{ reviewer_stars }}"></salla-rating-stars>
                      </div>
                    </div>
                  </div>
                </div>
              </article>
            </div>
          {% endfor %}
        </div>
      </salla-slider>
    {% else %}
      <div class="th-home-testimonials__empty">{{ empty_reviews_text }}</div>
    {% endif %}
  </div>
</section>
