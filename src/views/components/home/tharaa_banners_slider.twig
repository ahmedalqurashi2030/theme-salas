{# =========================================================
  Tharaa | Banners Slider (Home Component)
  - Uses official <salla-slider>
  - 1 banner per view (guaranteed)
========================================================= #}

{% set full_width   = component.full_width ?? true %}
{% set rounded      = component.rounded ?? true %}
{% set autoplay     = component.autoplay ?? true %}
{% set show_arrows  = component.show_arrows ?? true %}
{% set custom_class = component.custom_class ?? '' %}
{% set banners      = component.banners ?? [] %}

{% if banners|length > 0 %}
  {% set multiple = banners|length > 1 %}

  {# Swiper config (official via slider-config) #}
  {% set sliderConfig = {
    "slidesPerView": 1,
    "spaceBetween": 0,
    "centeredSlides": false,
    "watchOverflow": true,
    "loop": multiple,
    "autoplay": (autoplay and multiple) ? {"delay": 3500, "disableOnInteraction": false} : false
  } %}

  <section class="tharaa-banners-slider {{ custom_class }}{{ full_width ? ' is-fullwidth' : '' }}{{ rounded ? ' is-rounded' : '' }}">
    <div class="tharaa-banners-slider__wrap {{ full_width ? 'tharaa-banners-slider__wrap--full' : 'container' }}">

      <salla-slider
        class="tharaa-banners-slider__slider"
        type="carousel"
        slides-per-view="1"
        show-controls="{{ (show_arrows and multiple) ? 'true' : 'false' }}"
        auto-play="{{ (autoplay and multiple) ? 'true' : 'false' }}"
        loop="{{ multiple ? 'true' : 'false' }}"
        slider-config='{{ sliderConfig|json_encode|e("html_attr") }}'
      >
        {% for banner in banners %}

          {# image fields can be string or object -> normalize #}
          {% set img_desktop = banner.image ?? null %}
          {% if img_desktop and img_desktop.url is defined %}{% set img_desktop = img_desktop.url %}{% endif %}

          {% set img_mobile  = banner.mobile_image ?? null %}
          {% if img_mobile and img_mobile.url is defined %}{% set img_mobile = img_mobile.url %}{% endif %}

          {% set url = banner.url ?? null %}

          {% if img_desktop and url %}
            <div class="tharaa-banners-slider__slide" slot="items">
              <a class="tharaa-banners-slider__link" href="{{ url }}" aria-label="عرض البنر">
                <picture class="tharaa-banners-slider__media{% if img_mobile %} has-mobile{% endif %}">
                  {% if img_mobile %}
                    <source media="(max-width: 768px)" srcset="{{ img_mobile }}">
                  {% endif %}
                  <img
                    class="tharaa-banners-slider__img"
                    src="{{ img_desktop }}"
                    alt=""
                    loading="{{ loop.first ? 'eager' : 'lazy' }}"
                    decoding="async"
                    fetchpriority="{{ loop.first ? 'high' : 'auto' }}"
                  >
                </picture>
              </a>
            </div>
          {% endif %}

        {% endfor %}
      </salla-slider>

    </div>
  </section>
{% endif %}



  <style>
  /* ===========================================
   Tharaa | Banners Slider (Full-width, 1-slide)
   - Isolated (won't affect other sliders)
   - RTL ready
   - Forces 1 banner per view visually
=========================================== */

.tharaa-banners-slider {
  direction: rtl;
  position: relative;
  margin-top: 16px;
}

/* Wrap */
.tharaa-banners-slider__wrap {
  max-width: 1200px;
  margin-inline: auto;
  padding-inline: 16px;
}

.tharaa-banners-slider__wrap--full {
  max-width: 100%;
  padding-inline: 0;
}

/* Slider root */
.tharaa-banners-slider__slider {
  display: block;
  width: 100%;
}

/* Force each slide to take full width (critical) */
.tharaa-banners-slider__slide {
  width: 100% !important;
  max-width: 100% !important;
  flex: 0 0 100% !important;
}

.tharaa-banners-slider__link {
  display: block;
  width: 100%;
  overflow: hidden;
  text-decoration: none;
  -webkit-tap-highlight-color: transparent;

  /* Premium look */
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.10);
  transform: translateZ(0);
}

.tharaa-banners-slider.is-rounded .tharaa-banners-slider__link {
  border-radius: 18px;
}

.tharaa-banners-slider__media {
  display: block;
  width: 100%;
  aspect-ratio: 1300 / 300;
}

/* Make image fill the full banner area */
.tharaa-banners-slider__img {
  display: block;
  width: 100%;
  height: 100%;

  /* Desktop hero ratio like competitor */
  object-fit: cover;
  object-position: center;
}

/* Mobile: if you provide square mobile_image */
@media (max-width: 768px) {
  .tharaa-banners-slider {
    margin-top: 12px;
  }

  .tharaa-banners-slider__wrap {
    padding-inline: 12px;
  }

  .tharaa-banners-slider__wrap--full {
    padding-inline: 0;
  }

  .tharaa-banners-slider__link {
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.10);
  }

  .tharaa-banners-slider.is-rounded .tharaa-banners-slider__link {
    border-radius: 16px;
  }

  /* Using mobile image square will look perfect */
  .tharaa-banners-slider__media.has-mobile {
    aspect-ratio: 1 / 1;
  }
}
</style>
{% endif %}
